version: "3"

services:
  # db:
  #   image: postgres:latest
  #   ports:
  #       - "5432:5432"
  #   environment:
  #       - POSTGRES_USER=postgres
  #       - POSTGRES_PASSWORD=pasword
  #       - POSTGRES_DB= travel_on_rent

  webapp:
    image: golang:latest
    volumes:
      - .:/go/src/github.com/webapp
    env_file: .env
    command: go run main.go
    working_dir: /go/src/github.com/webapp
    ports:
      - "8080:8080"
      - "5000:5000"
    # depends_on:
    #   - db
